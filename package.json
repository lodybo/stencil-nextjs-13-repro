{
  "name": "nextjs-13-stencil-app",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "run-s build:component-lib build:component-lib-react",
    "build:component-lib": "npm run build -w component-lib",
    "build:component-lib-react": "npm run build -w component-lib-react",
    "dev": "npm run dev -w next-app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "./packages/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
